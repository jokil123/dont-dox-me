```json
{
  "globalSettings" : {

  }
  "rules": [
    //rule
    {
      "enabled": boolean,
      "type" : (Tel, IBAN, Email, Name, Address)
      "tests": string[]
      ""
    }
  ]
}
```

## StorageBuckets

dict

```
{
  rules: [int-ids...]
}
```
