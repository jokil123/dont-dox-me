import * as pageHide from "../censor/hideWebpage";

console.log("Page Hidden");

// hides the webpage because censoring / script execution might be too slow and reveal sensitive data
pageHide.hide();
